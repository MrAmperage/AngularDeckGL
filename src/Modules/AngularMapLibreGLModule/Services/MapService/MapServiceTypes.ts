export type LoaderOption = {
  /*Id лоадера должен быть одинаков с Id виджета с которым он взаимодействует  */
  Id: string;
  /*Отображается ли виджет ? */
  IsShow: boolean;
};

export type WidgetOption = {
  Id: string;
};
